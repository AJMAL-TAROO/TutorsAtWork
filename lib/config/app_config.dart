class AppConfig {
  const AppConfig._();

  static const appName = 'House Of Tutors';

  // Keep disabled until real Firebase options are supplied through
  // flutterfire configure or environment-specific configuration.
  static const enableFirebase = false;
}
